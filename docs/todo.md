# 진행중 / 다음 할 일

항상 최신 상태로 덮어쓴다. 작업을 시작하거나 다음 할 일을 정할 때 먼저 이 문서를 읽는다. 완료된 작업 이력은 docs/done.md 참고.

## 확인 필요
- 시간 청산 자동 매도는 아직 모의 실행만 확인했고, 사용자가 실제 실전 전환(.env에 DAMDAM_LIVE_ORDERS=true 추가) 전에 며칠 더 모의 실행 로그를 지켜보는 걸 권장
- 실제 주문으로 체결 이벤트 수신 확인은 아직 안 해봄 (급하지 않으면 넘어가도 됨)

## 다음 할 일
- ATR 기반 익절/손절 (조건주문 OCO)
  - 완료: 캔들 데이터 조회, 14일 ATR 계산 (`market` 패키지), 실제 계좌로 값 확인
  - 완료: 조건주문 OCO 등록/수정/취소/조회 API 클라이언트와 가격 계산 (`conditionalorder` 패키지). 모의 실행 기본값, 단위 테스트로 검증
  - 남음: 매수 체결 감지(웹소켓) 시 ATR 계산 → OCO 등록/기존 것 수정으로 자동 연결
  - 남음: 모의 실행으로 실제 계좌 전체 플로우(체결 감지 → OCO 등록/수정 로그) 확인
- 이후 2단계(백테스트)로 넘어갈지 사용자와 상의
