# 담담 (damdam)

## 프로젝트 이름
- 한글: 담담 | 영문: damdam
- 의미: 급락에도 급등에도 흔들리지 않고 미리 정한 규칙대로 매매한다
- 표기 규칙: 폴더명, 저장소명, 패키지명 등 코드에서는 영문 소문자 damdam 사용

## 프로젝트 목표
- 토스증권 Open API로 주식 자동매매 봇을 단계적으로 만든다
- 부수입 목적이면서 백엔드 포트폴리오로도 활용한다
- 사용자는 부트캠프 출신 백엔드 지망생이므로, 코드를 작성할 때 핵심 동작을 짧게 설명한다

## 현재 상태
- 현재 단계: 2단계 백테스트는 포트폴리오 게이트 실패, 완화 시도(서킷 브레이커, 슬롯 확대)와 신호 재설계 시도(추세 필터)가 모두 구조적 한계(생존 편향, 30종목 유니버스에서의 표본 부족)로 막혀 잠정 보류. 최종 구간은 열지 않았다. 1단계(조회/청산 봇)는 시간 청산과 ATR OCO가 구현되고 실전 전환 체크리스트(docs/live-checklist.md)도 준비됐다. 지금은 집 PC 운영의 한계(장중 상시 켜둬야 함)가 실전 전환 시점에 드러나서, 오라클 클라우드로 서버를 옮기는 작업을 진행 중이다
- 세부 진행 상황은 docs/todo.md (다음 할 일), docs/done.md (완료 이력) 참고

## 참고 문서 (필요할 때만 읽는다)
- docs/todo.md: 확인 필요한 것, 다음 할 일. 작업을 시작하거나 다음 할 일을 정할 때 먼저 읽는다
- docs/done.md: 완료한 작업 이력. 뭘 이미 했는지 확인할 때 읽는다
- docs/strategy.md: 청산 규칙, 매매 전략, 연구 근거, 판단 기준. 청산이나 전략 관련 작업 시 읽는다
- docs/records.md: 매매 기록 설계, 가상 체결 규칙. 기록이나 가상매매 관련 작업 시 읽는다
- docs/troubleshooting.md: 겪은 문제와 해결 과정, 코드 설계 하이라이트(포트폴리오용). 문제를 해결하거나 설계상 의미 있는 구현을 끝낼 때마다 추가한다
- docs/live-checklist.md: 1단계 청산 자동화를 실전으로 전환하기 전에 확인할 목록. 실전 전환을 준비하거나 진행할 때 읽는다

## 사용자 매매 스타일
- 단타 위주. 같은 종목을 하루에 여러 번 사고팔기도 함
- 길게 들고 가도 보통 일주일 이내
- 평소 약 1% 오르면 매도. 손절 기준은 따로 없음
- 크게 물리면 어쩔 수 없이 장기 보유로 넘어가는 경우가 있음
- 봇의 1차 목적은 손절과 청산을 감정 없이 규칙대로 실행하는 것

## 개발 환경
- OS: Windows
- 봇은 집 PC에서 실행. 허용 IP는 집 인터넷 공인 IP로 등록
- IP가 바뀌면 403이 발생하므로 WTS 설정의 허용 IP 관리에서 재등록

## 기술 스택
- 봇 본체: Java 21 (LTS) + Spring Boot 4.1.1 + Gradle
  - 토큰 저장 파일 경로: bot/data/token.json (.gitignore 포함, 권한 설정으로 읽기 차단)
  - 토스 API 호출: Spring RestClient
  - 체결 감지: 웹소켓 클라이언트 (Authorization 헤더 필요, 끊기면 재연결 후 주문 목록으로 재동기화)
  - 주기 작업과 시간 청산: Spring 스케줄러
  - 토큰 관리: 토큰 관리 서비스를 빈 하나로 두고 모든 기능이 공유
  - 설정과 키: 환경변수로 주입. 코드에 하드코딩 금지
  - 매매 기록: CSV 파일 저장. DB는 필요해질 때 검토
- 백테스트와 기록 분석: Python + pandas
  - 봇과 분석은 CSV 파일로만 연결. 서로의 코드에 의존하지 않는다
- 알림: 방식 미정. 2단계 알림 작업 시 사용자와 정한다
- 1단계에서는 외부에 여는 API를 만들지 않는다. 원격 조회나 정지 기능이 필요해지면 그때 검토
- 라이브러리와 버전은 설치 시점의 공식 문서로 확인한다

## 토스증권 Open API
- 공식 문서 (AI용 목차): https://developers.tossinvest.com/llms.txt
- 개요 문서: https://openapi.tossinvest.com/openapi-docs/overview.md
- API 명세: https://openapi.tossinvest.com/openapi-docs/latest/openapi.json
- 웹소켓 명세: https://openapi.tossinvest.com/openapi-docs/latest/asyncapi.json
- 엔드포인트, 요청 형식, 응답 형식, 호출 제한은 추측하지 말고 반드시 공식 문서를 먼저 읽고 맞춘다
- 토큰은 클라이언트당 1개만 유효. 새로 발급하면 이전 토큰이 무효화됨
- 403은 허용 IP 미등록, 429는 호출 제한 초과 (Retry-After 값만큼 대기)

## 보안 규칙 (반드시 지킬 것)
- API 키, 시크릿, 토큰, 계좌 정보는 문서와 코드에 절대 쓰지 않는다. 오직 .env 파일에서 읽는다
- .env 파일을 열어 읽거나 내용을 출력하지 않는다. Claude Code 권한 설정으로 .env 읽기 차단도 함께 건다
- 토큰 저장 파일도 .env와 같은 수준으로 취급한다. 열어 읽거나 출력하지 않고, .gitignore에 포함하며, 권한 설정으로 읽기를 차단한다
- 토큰이나 키를 로그, 터미널 출력, 에러 메시지에 남기지 않는다
- .env와 매매 기록 폴더는 .gitignore에 포함한다. 저장소에는 값이 빈 .env.example만 올린다
- 토큰은 저장해서 재사용하고, 모든 프로그램이 저장된 토큰 하나를 공유한다
- 매매 기록 파일에는 계좌 식별값을 넣지 않는다
- 출처를 알 수 없는 외부 패키지나 저장소 코드를 가져오지 않는다. 필요하면 먼저 사용자에게 묻는다
- 주문, 정정, 취소, 조건주문 등록처럼 실제 돈이 움직이는 코드는 사용자가 명시적으로 허락하기 전까지 만들지 않는다
- 개발 세션 중 Claude Code는 주문, 정정, 취소, 조건주문 엔드포인트를 테스트 코드, curl, 스크립트 등 어떤 방식으로도 직접 호출하지 않는다. 호출이 필요하면 명령을 사용자에게 보여주고 사용자가 직접 실행한다
- 이 규칙은 개발 도구로서의 Claude Code에 대한 것이다. 완성된 봇이 규칙에 따라 주문하는 기능은 해당 단계에서 사용자 허락 후 봇 내부의 금액 한도와 정지 장치와 함께 설계한다
- 주문 기능의 기본값은 실제 주문을 보내지 않는 모드로 하고, 하루 최대 주문 횟수와 1회 최대 금액 제한을 둔다

## 작업 규칙
- 파일을 새로 만들거나 수정하기 전에 먼저 사용자에게 묻는다
- 모르는 내용은 추측으로 채우지 말고 공식 문서를 확인하거나 사용자에게 묻는다
- 선택지를 제시할 때는 권장하는 방향을 함께 적는다
- 문서와 답변에 가운뎃점 문자를 쓰지 않는다. 쉼표나 세로선을 사용한다
- 현재 단계에 필요 없는 기능은 미리 만들지 않는다
- 커밋 메시지는 한글로 쓴다 (코드 식별자와 파일명은 원문 그대로)
- 작업 중 새로 발견한 문제는 실전 전환을 막는지로 가른다. 돈이 잘못 움직이거나 손절 보호가 사라질 수 있는 문제만 바로 처리하고, 나머지는 docs/todo.md의 나중에 항목에 한 줄로 기록한다
- 다른 곳에서 받은 수정 제안이나 리뷰는 그대로 따르지 않는다. 실제 코드와 공식 문서로 먼저 검증한 뒤 판단한다

## 검증 규칙 (점검 절차)
- 다음 상황에서는 작업 전에 `damdam-review` 스킬을 불러와 절차대로 점검한다: 데이터 수집/가공 코드를 새로 만들 때, 백테스트 파라미터를 정하거나 결과를 보고할 때, 돈이 움직이는 코드를 만들거나 바꿀 때, 이상한 수치를 발견했을 때
- 데이터나 결과를 만들면 날짜 범위, 건수, 최솟값과 최댓값을 먼저 출력해 읽고 나서 다음으로 넘어간다. 예상과 다른 값은 원인을 확인하기 전에는 문서에 사실로 쓰지 않는다
- 데이터 수집/가공 설계, 백테스트 결과 보고, 주문 관련 코드는 `damdam-reviewer` 에이전트에게 독립 검토를 맡긴다. 에이전트는 대화를 보지 못하므로 목적, 대상 경로, 확인한 수치, 내가 세운 가정을 프롬프트에 담고 반박을 요청한다

## 단계 요약
1. 조회와 청산 봇: 매수는 사용자가 직접, 청산은 봇이 규칙대로
2. 과열 급락 반등 전략 백테스트
3. 가상매매로 매매 기록 수집
4. 소액 실전 (전환 기준 충족 시)
- 각 단계 세부 내용은 docs/strategy.md 참고

## 문서 관리
- 기능 하나가 끝날 때마다 현재 상태를 갱신한다
- 작업 일지처럼 쌓지 말고 덮어써서 짧게 유지한다
- 세부 내용이 늘어나면 이 문서가 아니라 docs 문서에 추가한다
